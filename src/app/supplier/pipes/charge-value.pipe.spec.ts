import { ChargeValuePipe } from './charge-value.pipe';

describe('ChargeValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ChargeValuePipe();
    expect(pipe).toBeTruthy();
  });
});
