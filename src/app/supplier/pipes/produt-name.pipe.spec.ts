import { ProdutNamePipe } from './produt-name.pipe';

describe('ProdutNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ProdutNamePipe();
    expect(pipe).toBeTruthy();
  });
});
